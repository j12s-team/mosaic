{
  "_comment": "Smoke-test config: typechecks lib/*.ts in isolation, with only DOM + node types loaded. Useful to verify the agent + API-client logic without spinning up the full Next stack. Run: `npm run typecheck:libs`.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "es2022"],
    "skipLibCheck": true,
    "noEmit": true,
    "types": ["node"],
    "moduleResolution": "node"
  },
  "include": ["lib/**/*.ts"],
  "exclude": ["node_modules", "next-env.d.ts"]
}
